<%= form_with(model: vistum, local: true) do |form| %>
  <% if vistum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vistum.errors.count, "error") %> prohibited this vistum from being saved:</h2>

      <ul>
      <% vistum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tittle %>
    <%= form.text_field :tittle %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
